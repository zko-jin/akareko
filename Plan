Descriptions by users, get from X familiar nodes and display

Same for covers

Site for upload only, you can create a user and upload directly to the main node

Watch party for less popular stuff low on seeders

Delete database without deleting config -> Recreating the db doesn't restore the default user

Double page mode (check image proportion), strip mode
Zoom in/out

Content that's only different by the timestamp gets replaced with the newer one

Implement groups in a way that we could revoke access to posting on it without needing all members that post to have the main key

Automatically save torrents periodically and on exit

Hide/Highlight Tags

Bookmarks/Bookmark Folders/Notifications
Actively search for data on bookmarked content

Change the way we store magnets

Ratings (Stored only in relays)

Trust system

Sort content entries by enumeration
Add algo for next entry while reading
    - If multiple entries for the same enum are found prioritize the same uploader

Add compression to db

Routine
    - Every hour check for followed indexes
        - Main nodes should already be updated
        - Check who posted other contents and ask them for updates
    - Every 5 minutes check event log on main nodes

Verification tokens from trusted nodes to guarantee there's nothing bad in it



Offer a button to manually sync with a poster node on index pages

Option to run a minimal node, only serving it's own content (better for scans that users want to follow)

Option to add minimum timestamps so other peers can't just ask for too much content

Auto download following content

Server chat
